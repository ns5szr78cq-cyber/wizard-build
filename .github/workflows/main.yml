name: DooN_Dylib_Maker
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # تحديث للإصدار 4

      - name: Setup Theos
        run: |
          sudo git clone --recursive https://github.com/theos/theos.git /opt/theos
          sudo chown -R $(id -u):$(id -g) /opt/theos
          git clone https://github.com/theos/sdks.git /opt/theos/sdks

      - name: Build Library
        run: |
          export THEOS=/opt/theos
          # إنشاء الملفات الضرورية للبناء
          echo "ARCHS = arm64 arm64e
          TARGET = iphone:clang:latest:14.0
          include \$(THEOS)/makefiles/common.mk
          TWEAK_NAME = DooNUP
          DooNUP_FILES = Tweak.x
          DooNUP_CFLAGS = -fobjc-arc
          include \$(THEOS_MAKE_PATH)/tweak.mk" > Makefile

          # عملية التجميع
          make package FINAL_PACKAGE=1
          
      - name: Upload Finished Dylib
        uses: actions/upload-artifact@v4 # ده الحل! التحديث للإصدار 4
        with:
          name: DooN-Dylib-Fixed
          path: .theos/obj/debug/*.dylib